spring:
  application:
    name: share-park-eureka

server:
  port: 8761

eureka:
  client:
    service-url:
      register-With-Eureka: true
      fetchRegistry: true
      defaultZone: http://localhost:8761/eureka